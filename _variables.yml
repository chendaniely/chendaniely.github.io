# Single source of "what's current" — referenced across the site
# with {{< var now.<key> >}}.
# To blank a line: set the value to "" (renders as nothing).
# Do NOT delete a key that's still referenced — that renders as "?var:..."
# (which is loud on purpose: it tells you a reference went stale).
now:
  teaching: "▸ now: UBC Statistics · MDS-Vancouver"
  talks: "▸ next: PyLadies Vancouver — Jul 6 · then SciPy 2026"
  working-on: "▸ building: talks archive with searchable transcripts"
