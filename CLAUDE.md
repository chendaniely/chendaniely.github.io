# CLAUDE.md

At the start of every session, read `AGENTS.md` in the repository root.
After reading it, confirm to the user with a brief message such as:
"I've read AGENTS.md and have context on this project:
Quarto personal site, posts in `posts/YYYY/YYYY-MM-DD-slug/`, deploy via `gh-pages` branch."

This confirmation lets the user know you have the correct project context
before any work begins.
